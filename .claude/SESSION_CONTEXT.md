# Session Context — Julian Perez Photography Custom Site

A running brain-dump of the project so a fresh Claude session can hit the ground running. **Read AGENTS.md first** (Next.js 16, not 14/15 — APIs differ from training data).

---

## Who & what

**Julian Perez** — DMV-based professional photographer (weddings, portraits, events). Rebuilding `julianperezphotography.com` as a custom Next.js site to replace the old Adobe Portfolio site. The replacement is live and being polished.

**Primary domain:** `julianperezphotography.com` (Vercel-hosted).

## Stack

- **Next.js 16.2.2** (App Router, Turbopack) on **React 19** + **TypeScript**
- **Tailwind CSS v4** with custom CSS variables in `src/app/globals.css`
- **Resend** for inquiry + questionnaire email delivery
- **Square Appointments** embedded on `/book` (cannot use iframe `sandbox` — Square detects it and silently no-ops)
- **Pic-Time** embedded on `/client`
- **Google Places API (New)** for live reviews with manual-testimonial fallback
- **Sanity** wired up but dormant — content currently lives in `src/lib/content.ts`
- **Vercel Web Analytics** + GA4 (`G-PHCBCZ85RW`)

## Project layout

```
src/
├── app/
│   ├── about/                       About page
│   ├── api/inquire/                 Inquiry POST handler (Resend)
│   ├── api/questionnaire/           Questionnaire POST handler (Resend, schema-validated)
│   ├── book/                        Square Appointments embed
│   ├── client/                      Pic-Time gallery embed
│   ├── inquire/                     General inquiry form
│   ├── portfolio/[category]/        Dynamic portfolio category pages
│   ├── services/[category]/         Dynamic service + packages pages
│   ├── questionnaire/               Index page
│   ├── questionnaire/[service]/     Dynamic per-service questionnaire pages
│   ├── layout.tsx                   Global metadata + LocalBusiness JSON-LD
│   ├── page.tsx                     Home
│   ├── robots.ts                    robots.txt
│   └── sitemap.ts                   Auto-derived from visibleServices/visiblePortfolios + QUESTIONNAIRES
├── components/                      Nav, Footer, GoogleReviews, InquiryForm, QuestionnaireForm, etc.
└── lib/
    ├── content.ts                   Single source of truth: services, portfolios, settings (~1460 lines)
    ├── questionnaires.ts            Schema-driven planning questionnaires (~2400 lines, 13 schemas)
    ├── google-reviews.ts            Server-only Google Places fetcher
    ├── inline.tsx                   Markdown link → React renderer for cross-page link copy
    ├── portfolio-manifest.ts        Auto-generated by scripts/import-photos.mjs
    └── types.ts                     ServiceSlug union, Umbrella type, shared types
scripts/
└── import-photos.mjs                Lightroom-export → portfolio importer
public/portfolio/<slug>/             Imported gallery images
```

## Information architecture (current)

**14 visible services** organized into **4 umbrellas** (defined in `src/lib/types.ts`):

1. **Weddings & Couples** *(2)* — Weddings, Engagements & Couples
2. **Family & Life Events** *(6)* — Maternity, Newborn & First Year, Family Portraits, Family Celebrations, Pet Photography, Quinceañera & Cultural Milestones
3. **Personal & Pro Portraits** *(3)* — Portraiture, Graduation, Corporate Headshots
4. **Brand & Events** *(3)* — Corporate & Community Events, Brand & Commercial Content, Real Estate / Airbnb

Hidden but preserved: Modeling.

Portfolios mirror services 1:1. Both `services` and `portfolios` are tagged with an `umbrella` field so `Nav.tsx` and the index pages group automatically.

Top nav (after Round 5): `Portfolio ▾`, `Services & Pricing ▾`, `About`, `Clients`, `Inquire`, `Plan`, `Book` (solid pill).

## Past rounds — what's already shipped

**Round 1 — Scaffold.** Next.js + Tailwind, content in `src/lib/content.ts`, 9 initial categories, inquiry form via Resend, Square embed on /book, Pic-Time embed on /client.

**Round 2 — Pricing & portfolio restructure.** Flat 9 categories → 14 across 4 umbrellas. Split Family Events into Family Portraits + Family Celebrations. Renamed engagements → engagements-couples, promotional → brand-commercial, corporate-events → corporate-community-events. Added newborn, pet, quinceanera (later renamed `cultural-milestones`), real-estate. 308 redirects in `next.config.ts` for old slugs. Megamenu nav grouped by umbrella.

**Round 3 — Cross-page links, portfolio CTAs, wider Pic-Time, live Google reviews.** Added `src/lib/inline.tsx` markdown-link helper for cross-references in service-page copy. Added "View portfolio" buttons on pricing pages. Bumped `/client` from `max-w-5xl` → `max-w-7xl`. Built `src/lib/google-reviews.ts` (server-only, daily revalidate) + `<GoogleReviews>` component. Mounted on home, /inquire, /client. Graceful fallback when env vars unset.

**Round 4 — Planning questionnaires.** Schema-driven system in `src/lib/questionnaires.ts`. 10 questionnaires (weddings, cultural-milestones, family-celebrations, corporate-community-events, concerts-performances, maternity, newborn, portraiture, graduation, corporate-headshots). `<QuestionnaireForm>` client component with localStorage autosave, conditional `showIf` fields, prefill via query params. Routes: `/questionnaire`, `/questionnaire/[service]`, `/api/questionnaire` (Resend, dev console-log fallback).

**Round 4.5 — Scope-aware branching.** Extended `ShowIfClause` from `equals`-only to support `equals` / `equalsAny` / `notEquals` / `notEqualsAny`. Added section-level `showIf` (Section type now has `showIf`). Created shared `evaluateShowIf` and `visibleSectionsFor` helpers as single source of truth used by both client form and server validator (prevents drift). Wedding, cultural-milestones, family-celebrations, corporate-events, and concerts schemas got package-aware branches (e.g. wedding Mini elopement hides Reception/Getting-ready and shows an Elopement section).

**Round 5 — Visibility + 3 more questionnaires *(this session)*.**
- **Visibility:** Added `Plan` link to nav (between Inquire and Book, plain link not CTA — Book pill stays the single header CTA). Home page bottom CTA went 2 → 3 buttons with framing copy: "Have questions? Start with an inquiry. Already know which service you want? Jump straight into the planning questionnaire." `Plan your session` is the solid primary in the middle, Inquire + Book flank it as outline buttons. Inquire page intro now explicitly says "general inquiries" and includes a bordered callout above the form linking to `/questionnaire`. Book page replaced its plain-text "Already booked?" link with the same bordered callout pattern. Footer "For clients" column renamed `Planning questionnaires` → `Plan your session`. /questionnaire index intro strengthened to explicitly contrast with /inquire.
- **Three new questionnaires added** to `src/lib/questionnaires.ts` and registered in `QUESTIONNAIRES`:
  - `engagements-couples` (~8 min) — branches on session purpose. Surprise Proposal package shows a hide-and-coordinate logistics section; non-Surprise packages show a Session Planning section; Premium adds a Wedding Context section. Section counts: Surprise = 5, Premium = 6, Essentials = 5.
  - `family-portraits` (~7 min) — branches on group size. Premium adds an Extended Family Heads-Up section, an outfit-changes field, a wardrobe consult opt-in, and a Second Location field. Section counts: Premium = 8, Signature/Mini = 7.
  - `brand-commercial` (~9 min) — wildly different scopes per package. Class/Workshop section appears for `Class / Workshop Shoot` OR `Brand Half-Day`. Product/Lifestyle section appears for `Product / Lifestyle` OR `Brand Half-Day`. Storefront/Mini section only for `Brand Mini`. Half-Day Planning section only for `Brand Half-Day`. Section counts: Brand Half-Day = 9, Class/Workshop = 7, Product/Lifestyle = 7, Brand Mini = 7.

**13 questionnaires total** now: weddings, engagements-couples, cultural-milestones, family-portraits, family-celebrations, corporate-community-events, brand-commercial, concerts-performances, maternity, newborn, portraiture, graduation, corporate-headshots.

**Verified during Round 5:** `npm run build` clean. All three new routes return 200. Section-count branching matches expectations for all four brand-commercial packages, both engagements-couples branches, and both family-portraits branches.

## Critical conventions & gotchas

1. **Read `node_modules/next/dist/docs/` before writing framework code.** Next.js 16 has breaking changes. AGENTS.md is the canonical reminder.
2. **`searchParams` is async** in Next.js 16 — type as `Promise<...>` and `await sp` (see `src/app/inquire/page.tsx` for the pattern).
3. **Iframe `sandbox` is forbidden on /book** — Square detects sandboxed contexts and silently refuses to navigate. Same caveat applies if you ever wrap the Pic-Time iframe.
4. **Content is a single source of truth.** Add a new service: (1) `ServiceSlug` union in `src/lib/types.ts`, (2) `services` array in `src/lib/content.ts`, (3) `portfolios` array, (4) `KNOWN_SLUGS` in `scripts/import-photos.mjs`, (5) optional `makesOffer` entry in `src/app/layout.tsx`. Sitemap, JSON-LD, and `generateStaticParams` derive automatically.
5. **Add a new questionnaire:** define a `Questionnaire` constant in `src/lib/questionnaires.ts` (most start with `yourDetailsSection` and `bookingStatusSection(slug)` as sections 1–2), register it in the `QUESTIONNAIRES` map. Service page CTA, sitemap, and index page pick it up automatically.
6. **Visibility lives in one place.** `evaluateShowIf` and `visibleSectionsFor` (`src/lib/questionnaires.ts:~1947`) are used by both `<QuestionnaireForm>` and `/api/questionnaire/route.ts`. Don't duplicate the logic — call the helper.
7. **`showIf` semantics:**
   - `equals` / `equalsAny` → hidden until parent is answered.
   - `notEquals` / `notEqualsAny` → visible when parent is unset (so scope-narrowing rules don't blank the form before the user picks a package).
8. **The `package` field type** is auto-resolved at render-time from `services[slug].packages` via `resolvePackageOptions(slug)`. Never hard-code package option lists.
9. **Cross-page link copy** uses `[label](href)` markdown inside `comboNote` / `pricingNote` / intro strings. The `renderInline()` helper in `src/lib/inline.tsx` handles it.
10. **Google Reviews degrade gracefully.** If `GOOGLE_PLACES_API_KEY` or `GOOGLE_PLACE_ID` are unset, `<GoogleReviews>` returns `null`. Don't add error states for this.
11. **Date awareness.** Today is **2026-04-09** as of the last session. Convert relative dates to absolute when writing memory.

## Environment variables

| Variable | Purpose |
| --- | --- |
| `RESEND_API_KEY` | Sends inquiry + questionnaire emails. Without it, the API routes log to dev console. |
| `RESEND_FROM` | Verified sender (e.g. `Julian Perez Photography <hello@julianperezphotography.com>`) |
| `INQUIRY_TO` | Where inquiries land (defaults to `siteSettings.contactEmail`) |
| `GOOGLE_PLACES_API_KEY` | Live Google reviews. Without it: manual testimonials fallback. |
| `GOOGLE_PLACE_ID` | Real Place ID from Google's Place ID Finder (NOT a CID) |

## Key file paths to remember

- **Plan history:** `/Users/julianperez/.claude/plans/starry-yawning-journal.md` — five-round addendum log of every implementation plan, top-to-bottom history of the rebuild.
- **This session log:** `.claude/SESSION_CONTEXT.md` (you are here).
- **Project conventions for AI agents:** `AGENTS.md` (and `CLAUDE.md` which just `@AGENTS.md`).
- **Content source of truth:** `src/lib/content.ts`.
- **Questionnaire schemas + visibility helpers:** `src/lib/questionnaires.ts`.
- **Form renderer (single client component for all questionnaires):** `src/components/QuestionnaireForm.tsx`.
- **Form validator (server, mirrors client visibility logic):** `src/app/api/questionnaire/route.ts`.

## Open follow-ups (V2 — not yet started)

These were green-lit but not built:

1. **File uploads for mood boards / inspiration** — add `file` field type, wire to Vercel Blob via signed-URL handler. Most relevant for portraiture, weddings, cultural-milestones, brand-commercial, newborn, maternity.
2. **PDF export of submissions** — server-side PDF render via `@react-pdf/renderer`. Attach to Resend email + offer download from success card.
3. **Auto-send planning-call calendar link** — after submission, render a "Schedule a planning call?" CTA on the success card linking to a Square Appointments booking type for "Planning Call". May need a new `siteSettings.planningCallUrl`.

## Out of scope (don't suggest)

- Slack/Discord webhook for submissions (Julian gets the Resend email already).
- Database persistence for submissions (inbox is fine until volume justifies it).

## Memory system

Persistent memory lives at `/Users/julianperez/.claude/projects/-Users-julianperez-Documents-Development-Julian-Perez-Photography/memory/`. Index in `MEMORY.md`. Currently has `user_role.md` (Julian = DMV photographer rebuilding site) and `project_site_rebuild.md` (Next.js + Sanity rebuild). Update these as the project evolves; don't duplicate code-derivable facts.
